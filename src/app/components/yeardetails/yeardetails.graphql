query yearDetails($year:String!) {
  launchesPast(find: {launch_year:$year } ) {
    id
    mission_name
    links {
      flickr_images
      mission_patch_small
    }
    rocket {
      rocket_name
    }
    launch_date_utc
  }
}