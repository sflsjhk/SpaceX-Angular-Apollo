query LaunchDetails($id: ID!) {
  launch(id: $id) {
    id
    mission_name
    launch_success
    details
    links {
      flickr_images
      video_link
      mission_patch_small
    }
    rocket {
      rocket_name
    }
    launch_date_utc
  }
}
